'use client'

// Réutilise le même composant que la page admin
// L'API utilisée est /api/admin/agents/:id qui est accessible à tous les rôles authentifiés
export { default } from '@/app/admin/agents/[id]/page'
